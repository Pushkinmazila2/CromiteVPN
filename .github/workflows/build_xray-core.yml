name: Stage 1 - Build Xray AAR

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      package_suffix:
        description: 'Package suffix (random if empty)'
        required: false
        default: ''
  push:
    paths:
      - '.github/workflows/build_xray-core.yml'

jobs:
  build-xray:
    runs-on: self-hosted
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate package name
        id: identity
        run: |
          SUFFIX="${{ inputs.package_suffix }}"
          if [ -z "$SUFFIX" ]; then
            SUFFIX=$(head -c 100 /dev/urandom | tr -dc 'a-z' | head -c 8)
          fi
          PACKAGE="com.${SUFFIX}.browser"
          echo "package=${PACKAGE}" >> $GITHUB_OUTPUT
          echo "Package: ${PACKAGE}"

      - name: Build Docker image
        run: |
          sudo docker rmi xray-builder:latest || true
          sudo docker build -t xray-builder:latest xray-bridge/

      - name: Build Xray AAR
        run: |
          sudo docker run --rm \
            -v "$(pwd)/xray-bridge:/build" \
            -e PACKAGE_NAME="${{ steps.identity.outputs.package }}" \
            xray-builder:latest \
            bash -c "
            set -e

            echo '=== go mod tidy ===' &&
            go mod tidy &&

            echo '=== add mobile explicitly ===' &&
            go get golang.org/x/mobile/bind &&
            go get golang.org/x/mobile/cmd/gomobile &&

            echo '=== unpack mobile sources ===' &&
            MOBILE_VER=\$(go list -m -json all | python3 -c "
            import sys,json
            for line in sys.stdin.read().split('\n}\n'):
                try:
                    m=json.loads(line+'}')
                    if m.get('Path')=='golang.org/x/mobile':
                        print(m['Version'])
                        break
                except: pass
            ") &&
            echo \"mobile version: \$MOBILE_VER\" &&
            mkdir -p \$GOPATH/src/golang.org/x &&
            cp -r \$GOPATH/pkg/mod/golang.org/x/mobile@\${MOBILE_VER} \$GOPATH/src/golang.org/x/mobile &&
            chmod -R u+w \$GOPATH/src/golang.org/x/mobile &&

            echo '=== gomobile init ===' &&
            gomobile init &&

            echo '=== gomobile bind ===' &&
            gomobile bind \
              -target=android/arm64 \
              -androidapi 21 \
              -ldflags \"-X xraymobile.appPackageName=\${PACKAGE_NAME}\" \
              -o xraymobile.aar \
              .
          "
          du -sh xray-bridge/xraymobile.aar

      - name: Upload AAR
        uses: actions/upload-artifact@v4
        with:
          name: xraymobile-aar
          path: xray-bridge/xraymobile.aar
          retention-days: 7
      
      - name: Send Telegram notification
        if: always()
        run: |
          MESSAGE="Статус Action: ${{ job.status }}
          Репозиторий: ${{ github.repository }}
          Автор: ${{ github.actor }}
          Подробнее: https://github.com{{ github.repository }}/actions/runs/${{ github.run_id }}"

          curl -s -X POST "http://${{ secrets.MYSECRET_IP}}:8080/${{ secrets.MYSECRET_WAY }}/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_TO }}" \
            -d "text=Action status: ${{ job.status }}"